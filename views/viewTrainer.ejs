<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title><%= trainer.name %></title>
    </head>
    <body>
        <h1><%= title %></h1>
        <a href="/<%= trainer.id %>/update">Update Trainer</a><br>
        <form action="/<%= trainer.id %>/delete" method="post">
            <button type="submit" onclick="return confirm('Are you sure you want to delete this trainer?');">Delete</button>
        </form>
        <%- include("partials/trainer", {trainer: trainer}) %>
        <a href="/">Home</a>
    </body>
</html>
